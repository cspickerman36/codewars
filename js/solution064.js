// Capitalization and Mutability: Your coworker was supposed to write a simple helper function to capitalize a string (that contains a single word) before they went on vacation.

function capitalizeWord(word) {
    return word.charAt(0).toUpperCase() + word.slice(1);
     }