//Grasshopper - Combine strings: Create a function named combineNames that accepts two parameters (first and last name). The function should return the full name.

function combineNames(Pram, Dram){
    const ary = [Pram,Dram]
    return ary.join(' ')
    }